#pragma once

#include <cstdlib>
#include <string>
#include <vector>

namespace whirl::matrix::fault {

void RandomSplit(std::vector<std::string> pool, size_t lhs);

}  // namespace whirl::matrix::fault
